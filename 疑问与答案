一、对于第一题的疑问

Q:但是计算中(7-2)/2的值不为2.5而是2，确实不会漏东西吗？跟数学里的不一样

A:数组下标根本没有 4.5 这种东西！
下标只能是整数：2、3、4、5、6、7
你根本不可能访问  nums[4.5] ，所以必须取整数。
 
 知识点：1.return -1;//返回值为-1可以表明若输入值不为数组中的任意一个，也就是没找到的意思。
         2.二分查找 m = i  + (j - i) / 2;//相当于（i+j）/2，只不过为了防止超过最大阈值写的。  二分查找，必须记，设中间值从中间往其中一边找。
        3.注意是nums【m】，而不是单个m，也不是nums（m）。
 
2. 为什么取整=4 不会漏？
 
我们把区间  [2,7]  拆开看：
 
- 中间位置我们取 4
​
- 左边区间： [2, 3] 
​
- 右边区间： [5, 7] 
 
你看：
2、3、4、5、6、7 全部都被覆盖了，一个都没漏掉。




二、对于第二题的疑问：为什么只更改前k项而不改其他的项
一、先搞懂：题目到底要求你做什么？（核心误区在这里）
C 语言里的数组，一旦定义，总长度是固定的，无法真正 “物理删除” 元素让数组变短。所以这道题的 “移除”，根本不是让你把数组从[3,2,2,3]变成长度为 2 的[2,2]，题目给了明确的、可落地的要求，只有两点：
逻辑上：返回k，也就是数组中不等于val的元素总个数；
数组上：修改原数组，让数组的前k个位置，按顺序存放所有不等于val的元素，数组剩下的位置是什么值、数组总长度，完全不影响，题目也不关心。
你看题目里给的「用户评测」代码也写得很清楚：
它只校验你返回的k对不对；
它只把你数组的前k个元素拿出来排序，和预期答案对比；
前k个之外的元素，它完全不管。



三、对于第三题的疑问........................
Q:int* res = (int*)malloc(sizeof(int) * numsSize);这一步是什么意思，是固定格式吗，不懂malloc动态分配

A:
二、拆解这行代码（4 步走）
我们把这行代码像剥洋葱一样剥开来看：
c
运行
int* res = (int*)malloc(sizeof(int) * numsSize);
第 1 步：算一算你需要多大的地方
代码片段：sizeof(int) * numsSize
sizeof(int)：一个 “整数” 占多大地方？在大多数电脑上是 4 个字节。
numsSize：你要存多少个整数？（比如 5 个，或者 1000 个）。
相乘：就是总共需要的字节数。
大白话翻译：“一个人占 4 平米，现在有 numsSize 个人，帮我算一下总共需要多少平米的房子。”
第 2 步：向系统申请内存
代码片段：malloc(...)
malloc 是一个函数，全称是 Memory Allocate（内存分配）。
你把刚才算好的 “总面积” 塞给它，它就去内存里找一块这么大的空地。
返回值：它会把这块空地的 **“门牌号”（起始地址）** 给你。
大白话翻译：“喂，系统！我要 sizeof(int) * numsSize 这么大一块地，给我批一下！”
第 3 步：告诉系统这块地用来干嘛
代码片段：(int*)
malloc 虽然给了你地，但它不知道你要在这块地上盖房子还是种庄稼（它返回的是 void* 类型，即 “无类型指针”）。
(int*) 叫做强制类型转换。
意思是：“这块地我用来存 int 整数的，以后每 4 个字节算一个格子。”
大白话翻译：“这张地契（地址）我改一下，注明这块地是专门用来当整数仓库的！”
第 4 步：找个本子记下地契
代码片段：int* res = ...
int*：定义一个 “能存整数地址的指针变量”。
res：是 result（结果）的缩写，你可以叫它 p 或者 a，无所谓。
把 malloc 给你的地址存进 res 里。
大白话翻译：“我找了个本儿叫 res，把刚才申请到的仓库地址写在这个本儿上，以后好找。”

知识点：1.malloc动态分配格式：int* res = (int*)malloc(sizeof(int) * numsSize);//res是一个新数组。numsSize是旧数组长度(主函数里的)  如果你想存小数（double），就把所有的 int 换成 double：


四、对于第四题的疑问
Q：可不可以不加上int ans = INT_MAX;与 ans = fmin(ans, j - i + 1); ans == INT_MAX ? 0 : ans;直接return最小的
A：可以，但是麻烦
INT_MAX 那一套其实是程序员处理 “求最小值” 问题的标准偷懒写法。如果你非要避开它，我们可以用「标记法」或者「特殊初始值法」来实现。
强烈建议你习惯并掌握 INT_MAX 这种写法。

知识点：1.fmin(a,b)：C 语言<math.h>里的函数，作用是取两个数里更小的那个。前面要加上#include <math.h>
       2.int ans = INT_MAX;偷懒写法  INT_MAX是一个超大的数。如果循环结束后ans还是这个超大数，就说明全程没找到符合条件的子数组。我们的目标是找最小的子数组长度，初始时我们还没找到任何符合条件的子数组，所以把ans设成一个超大的数。这样后面只要找到一个符合条件的长度，就一定比这个超大数小，就能顺利更新ans。
如果循环结束后ans还是这个超大数，就说明全程没找到符合条件的子数组。前面要加上#include <limits.h>

五0...........
   知识点：1. while(scanf("%d %d",&a,&b)!=EOF)///符合题目的直至文件结束。
          2.把0-n下标的数组内的数都加起来，需要再增加一个数组假设b[],(原数组a[]),先设b[0]=0;再b[i+1]=a[i]+b[i];b[b+1]即是下标0-i的值；
要求从下标 a 到下标 b 的和区间[a,b]的和=preSum[b+1]−preSum[a]；

六.............
  知识点：1.可以增加 <stdlib.h> 头文件：但是我没加
我们使用了 abs() 函数来计算整数的绝对值，这个函数在 stdlib.h 中定义。如：int a = abs(total_sum - sum2 * 2);

